local ConveyorData = require(script.ConveyorData)
local DecorationData = require(script.DecorationData)
local DropperData = require(script.DropperData)

local ItemData = {
	Conveyor = ConveyorData,
	Decoration = DecorationData,
	Dropper = DropperData,
}

return ItemData
