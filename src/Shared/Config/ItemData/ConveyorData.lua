local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Types = require(script.Parent.Types)
local Items = ReplicatedStorage.Assets.items

local ConveyorData: { [string]: Types.ConveyorItem } = {}

return ConveyorData
